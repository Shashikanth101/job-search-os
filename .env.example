DATABASE_PATH=./data/jobs.db
CAREER_PAGES_JSON=[]

# LLM Configuration — swap provider without touching code
LLM_PROVIDER=anthropic         # anthropic | openai | groq | ollama
LLM_API_KEY=your_api_key_here  # not needed for ollama
LLM_MODEL=                     # optional — uses provider default if empty

# Optional dedicated resume LLM configuration — each value defaults to the main LLM config if unset
RESUME_LLM_PROVIDER=           # defaults to LLM_PROVIDER
RESUME_LLM_API_KEY=            # defaults to LLM_API_KEY
RESUME_LLM_MODEL=              # defaults to LLM_MODEL

# Firecrawl
FIRECRAWL_API_KEY=your_key_here

# Server
PORT=3001
